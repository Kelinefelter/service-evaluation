# Render Blueprint 部署配置
# 提交后 Render 自动创建 Web Service
services:
  - type: web
    name: service-evaluation-api
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
